---
phase: 10-journal-writer
plan: 03
type: execute
depends_on: ["10-01"]
files_modified:
  - src/cta_eta/data_collection/orchestration/weather_daemon.py
---

<objective>
Refactor WeatherDaemon to write via JournalWriter instead of ParquetWriter.

Purpose: Complete the clean break from per-poll Parquet writes for the weather daemon. Weather polling runs every 30 minutes (≥ default 15-minute journal rotation interval), so each poll always opens a fresh journal segment. Rotation logic is still correct — it just triggers on time not on write frequency.

Output: WeatherDaemon using JournalWriter, old Parquet write path fully removed.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-journal-writer/10-CONTEXT.md
@.planning/phases/10-journal-writer/10-01-SUMMARY.md
@src/cta_eta/data_collection/orchestration/weather_daemon.py
@src/cta_eta/data_collection/storage_cache/journal_writer.py

**Tech stack available:** Python 3.13+, JournalWriter (from 10-01)
**Established patterns:** WeatherDaemon extends AsyncBaseDaemon; storage initialized in __init__ at line ~141; write at line ~469

**Constraining decisions:**
- Clean break: remove all ParquetWriter imports and references; do not keep old path alongside
- Weather daemon calls `self.storage.append_batch(merged_records, dataset_name="weather")` — no metadata param was used for weather writes anyway (check line ~469 in weather_daemon.py to confirm, then update call signature to JournalWriter API)
- dataset_name stays "weather"
- JournalWriter.close() must be called on daemon shutdown — mirror the same teardown approach used in 10-02 for train daemon
- Weather polling interval (30 min) exceeds default journal rotation (15 min), so each write may open a new journal — this is correct behavior
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap ParquetWriter for JournalWriter in __init__ and type annotations</name>
  <files>src/cta_eta/data_collection/orchestration/weather_daemon.py</files>
  <action>
    1. Replace import: `from cta_eta.data_collection.storage_cache.storage import create_parquet_writer` → `from cta_eta.data_collection.storage_cache.journal_writer import create_journal_writer`
    2. Replace TYPE_CHECKING import: `from cta_eta.data_collection.storage_cache.storage import ParquetWriter` → `from cta_eta.data_collection.storage_cache.journal_writer import JournalWriter`
    3. In class body attribute annotation: `storage: ParquetWriter` → `storage: JournalWriter`
    4. In __init__ (around line 141): `self.storage = create_parquet_writer(config)` → `self.storage = create_journal_writer(config)`
    5. Add journal close on daemon stop: use the same teardown hook pattern as 10-02 (train daemon) — call `self.storage.close()` before the daemon exits.
  </action>
  <verify>uv run basedpyright src/cta_eta/data_collection/orchestration/weather_daemon.py 2>&1 | grep -E "error|warning" | head -20</verify>
  <done>No type errors. storage attribute typed as JournalWriter. create_journal_writer used in __init__.</done>
</task>

<task type="auto">
  <name>Task 2: Update weather write call and verify clean removal of ParquetWriter references</name>
  <files>src/cta_eta/data_collection/orchestration/weather_daemon.py</files>
  <action>
    1. Find the write call (around line 469): `self.storage.append_batch(merged_records, dataset_name="weather")` — verify the existing call signature. If it already has no metadata= kwarg, the call is already compatible with JournalWriter.append_batch and only type annotation changes are needed. If it does have metadata=, drop that kwarg.
    2. After swap, grep the file to confirm zero ParquetWriter/create_parquet_writer references remain.
    3. Run the full test suite to confirm no regressions: `uv run pytest` — if orchestration tests exist for WeatherDaemon, they should still pass (they mock storage, so swapping the type may require mock updates; fix any mock type mismatches).
  </action>
  <verify>
    grep -n "ParquetWriter\|create_parquet_writer" src/cta_eta/data_collection/orchestration/weather_daemon.py | wc -l  # expect 0
    uv run pytest -x -q 2>&1 | tail -20
  </verify>
  <done>Zero ParquetWriter references in weather_daemon.py. uv run pytest passes. Type checker clean.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run basedpyright src/cta_eta/data_collection/orchestration/weather_daemon.py` passes
- [ ] `grep -n "ParquetWriter\|create_parquet_writer" src/cta_eta/data_collection/orchestration/weather_daemon.py` returns no matches
- [ ] `grep -rn "ParquetWriter\|create_parquet_writer" src/cta_eta/data_collection/orchestration/` returns no matches in either daemon
- [ ] `uv run pytest -x -q` passes (full suite)
- [ ] Both daemons use JournalWriter: `grep -rn "JournalWriter\|create_journal_writer" src/cta_eta/data_collection/orchestration/` shows both daemon files
</verification>

<success_criteria>
- All tasks completed
- WeatherDaemon uses JournalWriter exclusively
- No ParquetWriter imports or references remain in weather_daemon.py
- Full test suite passes
- Phase 10 complete: both daemons write IPC journals, per-poll Parquet write path removed
</success_criteria>

<output>
After completion, create `.planning/phases/10-journal-writer/10-03-SUMMARY.md`:

---
phase: 10-journal-writer
plan: 03
subsystem: orchestration
requires: [10-01]
provides: [WeatherDaemon-journal]
affects: []
tech-stack:
  added: []
  patterns: [daemon storage swap pattern]
key-files:
  - src/cta_eta/data_collection/orchestration/weather_daemon.py
key-decisions:
  - Weather write interval (30 min) > journal rotation (15 min): each poll may open new journal (correct)
---

# Phase 10 Plan 03: WeatherDaemon Refactor Summary

**[Substantive one-liner here]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `src/cta_eta/data_collection/orchestration/weather_daemon.py` - Swapped ParquetWriter → JournalWriter

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Phase 10 complete. Both daemons write IPC journals. Ready for Phase 11: Data Compaction.
</output>
