---
phase: 10-journal-writer
plan: 02
type: execute
depends_on: ["10-01"]
files_modified:
  - src/cta_eta/data_collection/orchestration/train_position_daemon.py
---

<objective>
Refactor TrainPositionDaemon to write via JournalWriter instead of ParquetWriter.

Purpose: Complete the clean break from per-poll Parquet writes for the train daemon. Each 15-second poll appends to the current IPC journal file; rotation is handled automatically by JournalWriter.

Output: TrainPositionDaemon using JournalWriter, old Parquet write path fully removed.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-journal-writer/10-CONTEXT.md
@.planning/phases/10-journal-writer/10-01-SUMMARY.md
@src/cta_eta/data_collection/orchestration/train_position_daemon.py
@src/cta_eta/data_collection/storage_cache/journal_writer.py

**Tech stack available:** Python 3.13+, JournalWriter (from 10-01)
**Established patterns:** TrainPositionDaemon extends AsyncBaseDaemon; storage initialized in __init__; writes happen in _store_train_position_records

**Constraining decisions:**
- Clean break: remove all ParquetWriter imports and references; do not keep old path alongside
- Gap metadata (pending_gap_metadata) — the `metadata=` parameter is REMOVED from JournalWriter.append_batch; the pending_gap_metadata field can remain on the daemon for logging purposes only (it is already logged in _collect_train_positions_cycle before the write)
- dataset_name stays "train_positions" (same as before)
- JournalWriter.close() must be called on daemon shutdown — add to daemon teardown
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap ParquetWriter for JournalWriter in __init__ and type annotations</name>
  <files>src/cta_eta/data_collection/orchestration/train_position_daemon.py</files>
  <action>
    1. Replace import: `from cta_eta.data_collection.storage_cache.storage import create_parquet_writer` → `from cta_eta.data_collection.storage_cache.journal_writer import create_journal_writer`
    2. Replace TYPE_CHECKING import: `from cta_eta.data_collection.storage_cache.storage import ParquetWriter` → `from cta_eta.data_collection.storage_cache.journal_writer import JournalWriter`
    3. In class body attribute annotation: `storage: ParquetWriter` → `storage: JournalWriter`
    4. In __init__: `self.storage = create_parquet_writer(config)` → `self.storage = create_journal_writer(config)`
    5. Add journal close on daemon stop: in AsyncBaseDaemon (or TrainPositionDaemon), call `self.storage.close()` before the daemon exits. Check if AsyncBaseDaemon has a teardown hook (e.g., `_on_stop()` or `stop()`). If not, add a `_teardown()` override that calls `self.storage.close()`. Read daemon_async.py first to find the right hook.
  </action>
  <verify>uv run basedpyright src/cta_eta/data_collection/orchestration/train_position_daemon.py 2>&1 | grep -E "error|warning" | head -20</verify>
  <done>No type errors. storage attribute is typed as JournalWriter. create_journal_writer used in __init__.</done>
</task>

<task type="auto">
  <name>Task 2: Update _store_train_position_records to use JournalWriter API</name>
  <files>src/cta_eta/data_collection/orchestration/train_position_daemon.py</files>
  <action>
    Update _store_train_position_records:
    1. Change `self.storage.append_batch(records, dataset_name="train_positions", metadata=self.pending_gap_metadata)` to `self.storage.append_batch(records, dataset_name="train_positions")` — drop the metadata= kwarg (JournalWriter has no metadata param).
    2. The rest of the method (storage_failure_count tracking, logging, state update) stays identical — gap metadata is still logged via the existing log lines in _collect_train_positions_cycle, and pending_gap_metadata is still cleared after a successful write (this is correct behavior to preserve).
    3. Do NOT change any other logic. The storage_failure_count, storage_backoff_until, last_poll_timestamp updates all stay as-is.

    Note: Do NOT remove the pending_gap_metadata field from the daemon — it is still used for logging purposes even though it's no longer passed to storage.
  </action>
  <verify>uv run basedpyright src/cta_eta/data_collection/orchestration/train_position_daemon.py 2>&1 | grep -c "error" || echo "0 errors"</verify>
  <done>No type errors. append_batch call uses JournalWriter signature (no metadata= kwarg). File imports only JournalWriter/create_journal_writer, no ParquetWriter references remain.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run basedpyright src/cta_eta/data_collection/orchestration/train_position_daemon.py` passes
- [ ] `grep -n "ParquetWriter\|create_parquet_writer" src/cta_eta/data_collection/orchestration/train_position_daemon.py` returns no matches
- [ ] `grep -n "JournalWriter\|create_journal_writer" src/cta_eta/data_collection/orchestration/train_position_daemon.py` shows correct imports and usage
- [ ] `uv run pytest tests/data_collection/orchestration/ -v` passes (if orchestration tests exist)
</verification>

<success_criteria>
- All tasks completed
- TrainPositionDaemon uses JournalWriter exclusively
- No ParquetWriter imports or references remain in train_position_daemon.py
- Type checks pass
- Existing tests pass (run full test suite: uv run pytest)
</success_criteria>

<output>
After completion, create `.planning/phases/10-journal-writer/10-02-SUMMARY.md`:

---
phase: 10-journal-writer
plan: 02
subsystem: orchestration
requires: [10-01]
provides: [TrainPositionDaemon-journal]
affects: []
tech-stack:
  added: []
  patterns: [daemon storage swap pattern]
key-files:
  - src/cta_eta/data_collection/orchestration/train_position_daemon.py
key-decisions:
  - pending_gap_metadata retained on daemon for logging; not passed to JournalWriter
---

# Phase 10 Plan 02: TrainPositionDaemon Refactor Summary

**[Substantive one-liner here]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `src/cta_eta/data_collection/orchestration/train_position_daemon.py` - Swapped ParquetWriter → JournalWriter

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

10-03 (weather daemon refactor) can run in parallel. Both complete → Phase 10 done.
</output>
