---
phase: 01-foundation-configuration
plan: 01
type: execute
depends_on: []
files_modified: [src/cta_eta/config.py, config.toml, pyproject.toml]
---

<objective>
Build hybrid configuration system that separates secrets (.env) from operational settings (TOML).

Purpose: Establish clean separation between sensitive data (API keys) and operational parameters (polling intervals, retry counts, storage paths) to enable version-controlled configuration without exposing secrets.
Output: Working config loader that merges .env secrets with TOML operational settings, ready for daemon configuration.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/codebase/STACK.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
@src/cta_eta/api_train_position.py

**Tech stack available:**
- Python 3.13+ with modern syntax
- dotenv for .env loading (already in use)
- UV package manager

**Established patterns:**
- Snake_case naming for modules and functions
- Type hints required (modern Python 3.13 syntax with built-in generics)
- Environment variables via `load_dotenv()` and `os.getenv()`
- Double quotes for strings

**Constraining decisions:**
- Must use TOML for operational config (structured, version-controlled)
- Must keep secrets in .env (existing pattern, git-ignored)
- Must support Python 3.13+ only (no backward compatibility)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TOML configuration schema</name>
  <files>config.toml</files>
  <action>Create `config.toml` at project root with sections for: [collection] (polling intervals for trains ~15s and weather ~30min), [retry] (max attempts, backoff settings), [storage] (local path for Parquet files, partitioning strategy), [logging] (log level, JSON format flag), [cache] (TTL for static data like stations). Use sensible defaults: train_interval_seconds=15, weather_interval_minutes=30, max_retry_attempts=10, log_level="INFO". Add inline comments explaining each setting. Follow TOML best practices with clear section grouping.</action>
  <verify>File exists, valid TOML syntax: `python -c "import tomllib; tomllib.load(open('config.toml', 'rb'))"`</verify>
  <done>config.toml exists, parses without errors, contains all 5 sections with documented defaults</done>
</task>

<task type="auto">
  <name>Task 2: Implement hybrid config loader</name>
  <files>src/cta_eta/config.py</files>
  <action>Create `src/cta_eta/config.py` module that loads config.toml using Python's built-in tomllib (Python 3.11+), then overlays environment variables from .env. Return a typed dataclass or dict with nested structure matching TOML sections. Function signature: `def load_config() -> dict[str, dict[str, str | int | float]]`. Call `load_dotenv()` first (existing pattern), then load TOML, then create config dict with secrets from os.getenv() for API keys (CTA_API_KEY, CHIDATA_APP_TOK, CHIDATA_APP_SECRET). Use tomllib.load() not deprecated toml library. Add type hints for all functions. Follow existing code style (double quotes, snake_case, type hints).</action>
  <verify>Import works: `python -c "from src.cta_eta.config import load_config; c = load_config(); assert 'collection' in c and 'retry' in c"`, type checking passes: `uv run basedpyright src/cta_eta/config.py`</verify>
  <done>config.py loads TOML and .env, returns typed dict with all sections, validates without type errors, imports successfully</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -c "import tomllib; tomllib.load(open('config.toml', 'rb'))"` succeeds
- [ ] `python -c "from src.cta_eta.config import load_config; load_config()"` returns config dict
- [ ] `uv run basedpyright src/cta_eta/config.py` passes with no errors
- [ ] config.toml has all 5 sections: collection, retry, storage, logging, cache
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No TypeScript/type errors introduced
- TOML config is valid and documented
- Config loader merges .env + TOML correctly
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-configuration/01-01-SUMMARY.md`:

# Phase 1 Plan 1: Configuration System Summary

**Hybrid config system established with TOML operational settings and .env secrets**

## Accomplishments

- Created config.toml with 5 sections (collection, retry, storage, logging, cache)
- Implemented config loader merging TOML + .env
- Type-safe configuration loading with Python 3.13 features

## Files Created/Modified

- `config.toml` - Operational configuration with documented defaults
- `src/cta_eta/config.py` - Hybrid config loader using tomllib

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 01-02-PLAN.md (Structured Logging)
</output>
