---
phase: 01-foundation-configuration
plan: 02
type: execute
depends_on: []
files_modified: [src/cta_eta/logging.py, pyproject.toml]
---

<objective>
Implement structured JSON logging for production-grade observability and debugging.

Purpose: Replace ad-hoc print statements with structured logging that outputs JSON for easy parsing, filtering, and integration with log aggregation systems. Enable correlation of API calls with timestamps, request IDs, and response metadata.
Output: Reusable logging utilities that emit structured JSON logs with context (timestamps, request metadata, errors).
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/codebase/STACK.md
@.planning/codebase/CONVENTIONS.md
@src/cta_eta/api_train_position.py
@src/cta_eta/api_stations_weather.py

**Tech stack available:**
- Python 3.13+ standard library logging
- No logging framework yet (early stage)
- rich library used in devtools for console output

**Established patterns:**
- Snake_case naming
- Type hints required
- Double quotes for strings
- Decorator pattern (`@stamina.retry`, `@log_calls`)

**Constraining decisions:**
- Must use JSON format for structured logging (PROJECT.md requirement)
- Must support both development (human-readable) and production (JSON) modes
- No external logging framework dependencies initially (use stdlib)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up JSON structured logger</name>
  <files>src/cta_eta/logging.py</files>
  <action>Create `src/cta_eta/logging.py` module with function `setup_logger(name: str, log_level: str = "INFO", json_format: bool = True) -> logging.Logger` that configures Python's standard logging library. When json_format=True, use logging.Formatter to output JSON with fields: timestamp (ISO 8601), level, logger_name, message, and extra fields. When json_format=False, use human-readable format for development. Return configured logger instance. Use logging.StreamHandler for stdout. Add helper function `get_logger(name: str) -> logging.Logger` that returns module-specific logger. Follow stdlib logging best practices - avoid basicConfig, use proper handler setup. Use json.dumps() to format log records as JSON (no external dependencies). Example JSON output: {"timestamp": "2026-01-17T12:00:00Z", "level": "INFO", "logger": "cta_eta.api", "message": "API call started", "request_id": "abc123"}.</action>
  <verify>Import works: `python -c "from src.cta_eta.logging import setup_logger, get_logger; logger = setup_logger('test', json_format=True); logger.info('test message')"` outputs JSON, type check passes: `uv run basedpyright src/cta_eta/logging.py`</verify>
  <done>logging.py exports setup_logger and get_logger, JSON logs contain required fields, development mode works, no type errors</done>
</task>

<task type="auto">
  <name>Task 2: Create logging utilities for API calls</name>
  <files>src/cta_eta/logging.py</files>
  <action>Add decorator `@log_api_call` to logging.py that wraps API functions and logs: (1) before call: method, url, params; (2) after success: status_code, response_time_ms; (3) after error: error_type, error_message. Decorator should accept logger as parameter. Use functools.wraps to preserve function metadata. Capture timing with time.perf_counter(). Log at INFO level for success, WARNING for retries, ERROR for failures. Include structured fields like {"event": "api_call_start", "method": "GET", "url": "...", "params": {...}}. Add context manager `with log_context(**kwargs)` that adds extra fields to all logs within context (useful for request_id, trace_id). Use contextvars for thread-safe context storage. Example usage: `@log_api_call(logger) def get_train_position(...)`. Don't use external libraries - stdlib only (logging, functools, time, contextvars, json).</action>
  <verify>Decorator works: `python -c "from src.cta_eta.logging import log_api_call, setup_logger; logger = setup_logger('test'); @log_api_call(logger)\ndef test(): return 'ok'\ntest()"` produces before/after logs with timing</verify>
  <done>log_api_call decorator logs API lifecycle, timing works, context manager adds fields, no external dependencies, type-safe</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -c "from src.cta_eta.logging import setup_logger; setup_logger('test', json_format=True).info('test')"` outputs valid JSON
- [ ] `uv run basedpyright src/cta_eta/logging.py` passes with no errors
- [ ] Decorator `@log_api_call` logs before and after with timing
- [ ] JSON logs contain: timestamp, level, logger, message fields
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No type errors
- JSON logging works with structured fields
- API call decorator captures timing and metadata
- No external dependencies added (stdlib only)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-configuration/01-02-SUMMARY.md`:

# Phase 1 Plan 2: Structured Logging Summary

**JSON structured logging with API call tracking**

## Accomplishments

- Implemented JSON structured logger with development/production modes
- Created API call decorator with timing and metadata capture
- Added context manager for request correlation

## Files Created/Modified

- `src/cta_eta/logging.py` - Structured logging utilities

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 01-03-PLAN.md (Daemon Framework)
</output>
