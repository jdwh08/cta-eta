---
phase: 01-foundation-configuration
plan: 03
type: execute
depends_on: ["01-01", "01-02"]
files_modified: [src/cta_eta/daemon.py, pyproject.toml]
---

<objective>
Create base daemon framework for continuous 24/7 operation with lifecycle management.

Purpose: Establish foundation for long-running polling daemons (train positions, weather) with proper startup/shutdown, signal handling, and state preservation to ensure zero data loss during restarts.
Output: Reusable daemon base class that handles lifecycle, graceful shutdown, and state persistence.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/codebase/STACK.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/01-foundation-configuration/01-01-SUMMARY.md
@.planning/phases/01-foundation-configuration/01-02-SUMMARY.md

**Tech stack available:**
- Python 3.13+ with modern syntax
- Config system from Plan 01 (config.toml + .env loader)
- Structured logging from Plan 02 (JSON logger)

**Established patterns:**
- Snake_case naming
- Type hints required
- Class-based design acceptable for daemons
- Signal handling with stdlib signal module

**Constraining decisions:**
- Must support graceful shutdown (SIGTERM, SIGINT)
- Must preserve state on shutdown for restart continuity
- Must integrate with config system and structured logging
- No external daemon libraries (use stdlib signal, atexit, abc)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create base daemon class with lifecycle</name>
  <files>src/cta_eta/daemon.py</files>
  <action>Create abstract base class `BaseDaemon(ABC)` in `src/cta_eta/daemon.py` with lifecycle methods: `start() -> None` (main entry point), `run() -> None` (abstract - subclasses implement), `stop() -> None` (cleanup hook). Include `__init__(self, config: dict, logger: logging.Logger)` that stores config and logger. Add `running: bool` flag to control main loop. Implement `start()` to: (1) log startup, (2) set running=True, (3) call self.run(), (4) handle exceptions with logging. Use `@abstractmethod` for run(). Add type hints throughout. Follow existing code style (snake_case, double quotes, type hints). Example usage pattern: `class TrainPoller(BaseDaemon): def run(self): while self.running: ...`. Use abc.ABC for abstract base class, typing.Protocol NOT needed.</action>
  <verify>Import works: `python -c "from src.cta_eta.daemon import BaseDaemon; from abc import ABC"`, type check: `uv run basedpyright src/cta_eta/daemon.py`</verify>
  <done>BaseDaemon class exists, has lifecycle methods, abstract run() method, type-safe, follows patterns</done>
</task>

<task type="auto">
  <name>Task 2: Add signal handling for graceful shutdown</name>
  <files>src/cta_eta/daemon.py</files>
  <action>Add signal handling to BaseDaemon class: register handlers in `start()` for SIGTERM and SIGINT that call `self.stop()`. Implement `stop()` method that: (1) sets self.running=False, (2) logs shutdown event, (3) calls self._save_state() hook. Use signal.signal() to register handlers. Handler function should be: `def _signal_handler(self, signum: int, frame) -> None:` that logs signal received and calls stop(). Register in start() with: `signal.signal(signal.SIGTERM, self._signal_handler)`, `signal.signal(signal.SIGINT, self._signal_handler)`. Add docstring explaining that subclasses can override _save_state() for custom state preservation. Use stdlib signal module (no external deps). Make stop() idempotent (safe to call multiple times).</action>
  <verify>Signal handling works: Create test daemon, verify SIGTERM/SIGINT trigger stop(), `uv run basedpyright src/cta_eta/daemon.py` passes</verify>
  <done>Signal handlers registered, stop() method works, idempotent shutdown, logs signals, no type errors</done>
</task>

<task type="auto">
  <name>Task 3: Implement state preservation on shutdown</name>
  <files>src/cta_eta/daemon.py</files>
  <action>Add state preservation to BaseDaemon: (1) Add abstract method `_get_state(self) -> dict[str, str | int | float]` that subclasses implement to return current state (last poll time, next scheduled action, etc). (2) Add `_save_state(self) -> None` method that calls _get_state() and writes JSON to `.daemon_state/{self.__class__.__name__}.json`. Create directory if missing using pathlib.Path. (3) Add `_load_state(self) -> dict[str, str | int | float] | None` that reads state file if exists, returns None if missing or corrupt. Call _load_state() in __init__() to restore state. Use json.dump/load for serialization. Add error handling for file I/O (log errors, don't crash). Use pathlib for path operations. Type hint state as dict[str, str | int | float]. Make _get_state() abstract so subclasses must implement it. Add docstring examples showing how to use state (e.g., "last_poll_timestamp", "next_weather_check").</action>
  <verify>State save/load works: `python -c "from src.cta_eta.daemon import BaseDaemon; import json; assert json.load(open('.daemon_state/test.json')) if Path('.daemon_state/test.json').exists() else True"`, type check passes</verify>
  <done>State preservation works, _get_state() abstract, _save_state() writes JSON, _load_state() restores, error handling present, type-safe</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -c "from src.cta_eta.daemon import BaseDaemon"` imports successfully
- [ ] `uv run basedpyright src/cta_eta/daemon.py` passes with no errors
- [ ] BaseDaemon has abstract run() and _get_state() methods
- [ ] Signal handlers (SIGTERM, SIGINT) trigger graceful shutdown
- [ ] State save/load methods work with JSON serialization
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No type errors
- BaseDaemon abstract class works as framework
- Signal handling triggers graceful shutdown
- State preservation saves/restores daemon state
- Ready for Phase 2 storage implementation
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-configuration/01-03-SUMMARY.md`:

# Phase 1 Plan 3: Daemon Framework Summary

**Base daemon class with lifecycle, signals, and state preservation**

## Accomplishments

- Created BaseDaemon abstract class with start/run/stop lifecycle
- Implemented SIGTERM/SIGINT signal handling for graceful shutdown
- Added state persistence to `.daemon_state/` for restart continuity

## Files Created/Modified

- `src/cta_eta/daemon.py` - Base daemon framework class

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Phase 1 complete, ready for Phase 2 (Storage Abstraction Layer)
</output>
