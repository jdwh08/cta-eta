---
phase: 12-storage-optimization
plan: "02"
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - src/cta_eta/data_collection/compaction/compact.py
  - tests/data_collection/compaction/test_compact.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "compaction reads registry and validates each journal schema before concat"
    - "breaking schema drift triggers email alert (same alerting path as upload failure)"
    - "only first breaking journal per day triggers alert (subsequent journals in same run: no repeat alert)"
    - "all journals are still merged regardless of drift (continue-on-drift policy)"
    - "merged Parquet file has schema_drift=true metadata when any journal had breaking drift"
    - "merged Parquet file has drift_summary metadata with field-level diff"
    - "additive drift (new field) is logged at INFO level, no alert"
    - "bootstrap creates registry on first successful compaction (no pre-existing registry)"
  artifacts:
    - path: "src/cta_eta/data_collection/compaction/compact.py"
      provides: "Schema validation loop in _compact_one_daemon before pa.concat_tables()"
      contains: "schema_registry"
    - path: "tests/data_collection/compaction/test_compact.py"
      provides: "Integration tests for drift detection in compaction"
  key_links:
    - from: "compact.py _compact_one_daemon"
      to: "schema_registry.classify_drift"
      via: "called per journal before concat"
      pattern: "classify_drift"
    - from: "compact.py _compact_one_daemon"
      to: "send_drift_alert"
      via: "called when first breaking drift detected"
      pattern: "send_drift_alert"
    - from: "compact.py _compact_one_daemon"
      to: "merged.replace_schema_metadata"
      via: "annotates Parquet table before write"
      pattern: "schema_drift.*true|drift_summary"
    - from: "compact.py _compact_one_daemon"
      to: "bootstrap_registry"
      via: "called after successful concat if registry missing"
      pattern: "bootstrap_registry"
---

<objective>
Integrate schema registry into the compaction pipeline: per-journal drift checking, drift alerting, Parquet file-level drift annotation, and registry bootstrap.

Purpose: This is the runtime enforcement of schema contracts. Operators are alerted when upstream APIs (CTA or weather) change their field structure.
Output: compact.py extended with schema validation loop, drift alert function, and file-level metadata annotation.
</objective>

<execution_context>
@/home/jdwh08/.claude/get-shit-done/workflows/execute-plan.md
@/home/jdwh08/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-storage-optimization/12-RESEARCH.md
@.planning/phases/12-storage-optimization/12-01-SUMMARY.md
@src/cta_eta/data_collection/compaction/compact.py
@src/cta_eta/data_collection/compaction/schemas.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema validation loop and drift alerting in compact.py</name>
  <files>src/cta_eta/data_collection/compaction/compact.py</files>
  <action>
Modify _compact_one_daemon to integrate schema registry validation. Changes:

1. Add imports at top of file:
   - from cta_eta.data_collection.compaction.schema_registry import (
       classify_drift, bootstrap_registry, load_registry, save_registry
     )
   - from pathlib import Path (already imported)

2. Add module-level constant for registry path base:
   _REGISTRY_DIR = Path(__file__).resolve().parents[3] / "schemas"
   (This resolves to src/cta_eta/schemas/ — same location as schema files)

3. Add a helper function send_drift_alert(drift_result, daemon_name, date_str, config):
   - Uses same alerting pattern as send_compaction_alert (reuse _build_email_config + send_email_alert)
   - Email subject: f"CTA Schema Drift Detected: {daemon_name} {date_str}"
   - Email body: full field-level diff:
     - "Breaking schema drift detected for daemon '{daemon_name}' on {date_str}.\n\n"
     - For each breaking_field: f"  CHANGED: {f.name}  {f.old_type} → {f.new_type}\n"
     - For each removed field: f"  REMOVED: {name}\n"
     - For each nullability_change: f"  NULLABILITY: {f.name}  nullable={f.old_nullable} → nullable={f.new_nullable}\n"
     - "\nNote: Compaction continued. The merged Parquet file is annotated with schema_drift=true.\n"
     - "To resolve: review the drift, then run: cta-compact schema update\n"
   - Graceful degradation: if alerting section missing, log warning and return (same as send_compaction_alert)

4. In _compact_one_daemon, BEFORE the existing "Step 3: Validate schema" block:

   After journal_files discovered (step 1) and before reading journals, load the registry:
     registry_path = _REGISTRY_DIR / f"{daemon_name}.json"
     registry_schema = load_registry(registry_path)  # None if no registry yet

5. In the per-journal loop (currently reads + validates against TRAIN_POSITION_SCHEMA), REPLACE the existing Step 3 schema check with the new drift-aware check:

   REMOVE the old strict equality check:
     if expected_schema is not None and not table.schema.equals(expected_schema):
         ... journals_skipped += 1; continue

   REPLACE with:
     # Schema drift check (using registry if available; fall back to hard-coded schema)
     check_schema = registry_schema if registry_schema is not None else expected_schema
     if check_schema is not None:
         drift = classify_drift(check_schema, table.schema)
         if drift.drift_type == "breaking":
             if not drift_alert_sent:  # only alert on first breaking journal
                 send_drift_alert(drift, daemon_name, date_str, config)
                 drift_alert_sent = True
                 _log.warning(
                     "Breaking schema drift in %s (first occurrence for %s %s)",
                     journal_path.name, daemon_name, date_str
                 )
             day_has_drift = True
             day_drift_result = drift  # keep last drift for annotation
         elif drift.drift_type == "additive":
             _log.info(
                 "Additive schema drift in %s (new fields: %s)",
                 journal_path.name,
                 [f.name for f in drift.added_fields]
             )
         # In all drift cases: still append table (continue merging per context decision)

   Initialize before the loop:
     drift_alert_sent = False
     day_has_drift = False
     day_drift_result = None

6. After pa.concat_tables() and pq.write_table(), before upload, annotate if drift:
   if day_has_drift and day_drift_result is not None:
       existing_meta = merged.schema.metadata or {}
       breaking = [
           {"name": f.name, "old_type": f.old_type, "new_type": f.new_type}
           for f in day_drift_result.breaking_fields
       ]
       removed = [name for name in day_drift_result.removed_fields]
       nullability = [
           {"name": f.name, "old_nullable": f.old_nullable, "new_nullable": f.new_nullable}
           for f in day_drift_result.nullability_changes
       ]
       drift_summary = json.dumps({
           "breaking_fields": breaking,
           "removed_fields": removed,
           "nullability_changes": nullability,
       })
       merged = merged.replace_schema_metadata({
           **existing_meta,
           b"schema_drift": b"true",
           b"drift_summary": drift_summary.encode(),
       })
       # Rewrite local Parquet with annotation
       pq.write_table(merged, local_parquet, compression="snappy")
       upload_bytes = local_parquet.stat().st_size  # update after rewrite

7. After successful upload (before archive), bootstrap registry if not yet present:
   bootstrap_registry(registry_path, merged.schema, daemon_name)
   If the registry already existed, bootstrap_registry is a no-op (returns False).

Note: Use promote_options="default" in pa.concat_tables(tables, promote_options="default") to handle additive drift (new fields filled with null). Change the existing concat_tables() call to include this parameter.

For handling type-incompatible fields during concat with breaking drift: cast breaking columns to the registry/expected type before concat. Before appending each table in the loop, if day_has_drift and a column type is incompatible with registry, cast it:
  For each field in (day_drift_result.breaking_fields if day_drift_result else []):
      if field.name in table.schema.names:
          col_idx = table.schema.get_field_index(field.name)
          registry_field = check_schema.field(field.name)
          try:
              casted_col = table.column(col_idx).cast(registry_field.type)
              table = table.set_column(col_idx, field.name, casted_col)
          except pa.ArrowInvalid:
              _log.warning("Cannot cast %s from %s to %s — keeping as-is",
                           field.name, field.new_type, field.old_type)
  </action>
  <verify>
`uv run basedpyright src/cta_eta/data_collection/compaction/compact.py` — 0 errors
`uv run python -m cta_eta.data_collection.compaction.compact --help` — shows usage (no import errors)
  </verify>
  <done>compact.py imports schema_registry functions, has send_drift_alert(), _compact_one_daemon uses classify_drift per journal, annotates merged table with schema_drift metadata when drift detected, bootstraps registry after first successful compaction.</done>
</task>

<task type="auto">
  <name>Task 2: Integration tests for drift detection in compaction</name>
  <files>tests/data_collection/compaction/test_compact.py</files>
  <action>
Add 3 new test classes to the existing test_compact.py. Keep all existing 4 tests intact.

Test class TestDriftAlertOnBreakingDrift:
  - Create 2 temp IPC journal files using pyarrow.ipc:
    - journal_1: uses registry schema (TRAIN_POSITION_SCHEMA)
    - journal_2: uses drifted schema (TRAIN_POSITION_SCHEMA with "route" field changed from pa.string() to pa.int64())
  - Patch discover_journals to return both journal paths
  - Patch load_registry to return TRAIN_POSITION_SCHEMA
  - Patch upload_parquet to succeed
  - Patch send_drift_alert to capture calls
  - Call _compact_one_daemon(...)
  - Assert send_drift_alert called exactly once (not twice — only first breaking journal alerts)
  - Assert returned metrics.status == "success" (compaction continues)

Test class TestDriftAnnotationInParquet:
  - Same setup as above
  - Instead of patching send_drift_alert, let it run but patch send_email_alert to no-op
  - After _compact_one_daemon returns, read local staging Parquet metadata:
      import pyarrow.parquet as pq
      meta = pq.read_metadata(local_parquet_path)
      assert meta.metadata.get(b"schema_drift") == b"true"
      assert b"drift_summary" in meta.metadata
  - Assert drift_summary JSON parses correctly and has "breaking_fields" key

Test class TestBootstrapOnFirstRun:
  - Use matching journals (no drift)
  - Patch load_registry to return None (no registry yet)
  - Patch upload_parquet to succeed
  - Patch bootstrap_registry to capture calls
  - Call _compact_one_daemon(...)
  - Assert bootstrap_registry called with correct registry_path and a pa.Schema

Note: For creating temp IPC files in tests, use this pattern:
  import pyarrow as pa
  import pyarrow.ipc as ipc
  import tempfile
  from pathlib import Path

  def _write_temp_ipc(path: Path, schema: pa.Schema, rows: int = 5) -> None:
      table = pa.table({
          name: pa.array([None] * rows, type=field.type)
          for name, field in zip(schema.names, schema)
      })
      with ipc.new_file(path, schema) as writer:
          writer.write_table(table)

Use unittest.mock.patch for all external dependencies (load_registry, upload_parquet, send_drift_alert, bootstrap_registry as needed).
  </action>
  <verify>
`uv run pytest tests/data_collection/compaction/test_compact.py -v` — all existing 4 tests + 3 new tests pass (7 total)
`uv run pytest` — full suite passes, no regressions
  </verify>
  <done>7 tests pass in test_compact.py. Drift alert fires once for breaking drift. Parquet metadata contains schema_drift=true annotation. Bootstrap called on missing registry. All original 4 tests still pass.</done>
</task>

</tasks>

<verification>
- `uv run basedpyright src/cta_eta/data_collection/compaction/` — 0 errors across all modules
- `uv run pytest tests/data_collection/compaction/ -v` — all tests pass
- `uv run pytest` — full test suite passes
- Manual check: compact.py has schema_registry imports, drift annotation code, bootstrap call after upload
</verification>

<success_criteria>
compact.py integrates schema_registry without regressions. Drift detection runs per journal. Breaking drift triggers email alert exactly once per day. Merged Parquet annotated with schema_drift=true on breaking drift. Registry bootstrapped on first successful run.
</success_criteria>

<output>
After completion, create `.planning/phases/12-storage-optimization/12-02-SUMMARY.md`
</output>
