---
phase: 12-storage-optimization
plan: "03"
type: execute
wave: 3
depends_on:
  - 12-01
  - 12-02
files_modified:
  - src/cta_eta/monitoring/cli.py
  - src/cta_eta/data_collection/compaction/compact.py
  - tests/monitoring/test_cli.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "cta-monitor compaction shows Schema column with OK or DRIFT for each row in the recent window"
    - "cta-compact schema update reads the latest compacted Parquet and overwrites the registry file"
    - "cta-compact schema update attempts git commit of the registry file, falls back to printing path on failure"
    - "cta-monitor compaction shows DRIFT for files annotated with schema_drift=true metadata"
    - "cta-monitor compaction shows OK for files with no schema_drift metadata"
  artifacts:
    - path: "src/cta_eta/monitoring/cli.py"
      provides: "Schema column in cmd_compaction table reading schema_drift from Parquet metadata"
      contains: "schema_drift"
    - path: "src/cta_eta/data_collection/compaction/compact.py"
      provides: "cta-compact schema update subcommand"
      contains: "schema update"
    - path: "tests/monitoring/test_cli.py"
      provides: "Tests for Schema column rendering and schema update command"
  key_links:
    - from: "cli.py cmd_compaction"
      to: "pyarrow.parquet.read_metadata"
      via: "reads Parquet footer metadata for each file in window"
      pattern: "pq.read_metadata|schema_drift"
    - from: "compact.py cmd_schema_update"
      to: "schema_registry.save_registry"
      via: "writes updated registry from latest Parquet schema"
      pattern: "save_registry"
    - from: "compact.py cmd_schema_update"
      to: "git commit"
      via: "subprocess.run git add + git commit"
      pattern: "subprocess.run.*git"
---

<objective>
Surface schema drift in cta-monitor CLI and add `cta-compact schema update` operator command for promoting a drifted schema to canonical.

Purpose: Closes the operator workflow loop — drift is visible in the monitoring dashboard and can be resolved with a single command that auto-commits the registry update to git.
Output: Schema column in cta-monitor compaction view; schema update subcommand in cta-compact.
</objective>

<execution_context>
@/home/jdwh08/.claude/get-shit-done/workflows/execute-plan.md
@/home/jdwh08/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-storage-optimization/12-RESEARCH.md
@.planning/phases/12-storage-optimization/12-02-SUMMARY.md
@src/cta_eta/monitoring/cli.py
@src/cta_eta/data_collection/compaction/compact.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema column in cta-monitor compaction view</name>
  <files>src/cta_eta/monitoring/cli.py</files>
  <action>
Modify cmd_compaction in cli.py to add a "Schema" column reading drift metadata from local Parquet files.

1. In cmd_compaction, after building the `filtered` list of sidecar records, add a helper to look up schema drift status for each record:

   def _read_schema_drift(compaction_dir: Path, daemon: str, date_str: str) -> str:
       """Read schema_drift metadata from local staging Parquet. Returns 'DRIFT', 'OK', or '?'."""
       parquet_path = compaction_dir / daemon / f"date={date_str}" / "data.parquet"
       if not parquet_path.exists():
           return "?"
       if pq is None:
           return "?"
       try:
           meta = pq.read_metadata(parquet_path)
           kv = meta.metadata or {}
           drift_val = kv.get(b"schema_drift", b"").decode()
           return "DRIFT" if drift_val == "true" else "OK"
       except Exception:  # noqa: BLE001
           return "?"

   Place this as a module-level function (not nested) so it is testable.

2. Update the table header print in cmd_compaction (human-readable path only):
   - Add "Schema" column at the end
   - New header line:
     f"{'Date':<12} {'Daemon':<20} {'Status':<10} {'Journals':<16} "
     f"{'Rows':<12} {'Upload':<10} {'Elapsed':<8} {'Schema':<8}"
   - Separator: "─" * 98 (extend to fit)

3. In the per-record loop, compute schema_str:
   schema_str = _read_schema_drift(compaction_dir, daemon_val, date_val)

4. Update the print statement to include schema_str:
   f"{date_val:<12} {daemon_val:<20} {status_str:<10} {journals_str:<16} "
   f"{rows_str:<12} {upload_str:<10} {elapsed_str:<8} {schema_str:<8}"

5. The json_output path does NOT need Schema column (JSON already includes all sidecar fields; drift annotation is on Parquet file, not sidecar). No change to JSON output path.

Note: _DEFAULT_COMPACTION_DIR is already defined in the module. Use it to resolve the Parquet path.
  </action>
  <verify>
`uv run basedpyright src/cta_eta/monitoring/cli.py` — 0 errors
`uv run cta-monitor compaction --help` — shows usage (no import errors)
  </verify>
  <done>cli.py has _read_schema_drift() function, cmd_compaction table shows Schema column, column shows DRIFT for annotated files and OK for clean files.</done>
</task>

<task type="auto">
  <name>Task 2: cta-compact schema update subcommand</name>
  <files>src/cta_eta/data_collection/compaction/compact.py</files>
  <action>
Add `cta-compact schema update` nested subcommand. The operator runs this to promote the currently observed (drifted) schema to canonical in the registry.

INTENTIONAL BREAKING CHANGE: The old `cta-compact --reprocess DATE` invocation is replaced by
`cta-compact run --reprocess DATE`. The systemd service (uses `cta-compact` with no subcommand)
is unaffected. Manual reprocessing operators must use the new form. Add a comment in main() noting
this change.

1. Add cmd_schema_update function:

   def cmd_schema_update(args: argparse.Namespace) -> None:
       """Promote latest observed Parquet schema to the registry.

       Finds the most recent compacted Parquet, reads its schema, overwrites the
       registry file for the given daemon, and attempts a git commit.

       Args:
           args: Parsed CLI args. Uses args.daemon (default: all daemons).
       """
       import subprocess  # stdlib, only needed here

       config = load_config()
       compaction_cfg = config.get("compaction", {})
       compaction_dir = Path(str(compaction_cfg.get("compaction_dir", "data/compaction")))

       daemon_names = ["train_positions", "weather"] if args.daemon == "all" else [args.daemon]

       for daemon_name in daemon_names:
           # Find most recent local Parquet for this daemon
           daemon_dir = compaction_dir / daemon_name
           if not daemon_dir.exists():
               print(f"No compaction data found for {daemon_name} in {daemon_dir}")
               continue

           # Sort date=YYYY-MM-DD dirs descending, take first
           date_dirs = sorted(daemon_dir.glob("date=*"), reverse=True)
           latest_parquet = None
           for date_dir in date_dirs:
               candidate = date_dir / "data.parquet"
               if candidate.exists():
                   latest_parquet = candidate
                   break

           if latest_parquet is None:
               print(f"No Parquet file found for {daemon_name}")
               continue

           # Read schema from latest Parquet
           schema = pq.read_schema(latest_parquet)
           registry_path = _REGISTRY_DIR / f"{daemon_name}.json"
           save_registry(registry_path, schema, daemon_name)
           print(f"Registry updated: {registry_path}")

           # Attempt git commit
           try:
               subprocess.run(
                   ["git", "add", str(registry_path)],
                   check=True, capture_output=True
               )
               subprocess.run(
                   ["git", "commit", "-m", f"chore: update schema registry for {daemon_name}"],
                   check=True, capture_output=True
               )
               print(f"Registry committed to git: {registry_path}")
           except (subprocess.CalledProcessError, FileNotFoundError):
               print(f"Registry updated. Commit manually:")
               print(f"  git add {registry_path} && git commit -m 'chore: update schema registry for {daemon_name}'")

2. Modify main() to support the schema subcommand. The existing main() uses a flat argparse parser with --reprocess. Add nested subcommands:

   Structure:
     cta-compact run [--reprocess YYYY-MM-DD]   (explicit run subcommand, replaces old --reprocess)
     cta-compact schema update [--daemon DAEMON]  (new)
     cta-compact (no args)                        (backward compat: runs compaction, same as systemd service)

   Implementation approach: Use subparsers but make compaction the default when no subcommand is given:

   parser = argparse.ArgumentParser(prog="cta-compact", ...)
   subparsers = parser.add_subparsers(dest="subcommand")

   # "run" subcommand (explicit, replaces old top-level --reprocess)
   # NOTE: 'cta-compact --reprocess DATE' no longer works; use 'cta-compact run --reprocess DATE'
   run_parser = subparsers.add_parser("run", help="Run daily compaction (default)")
   run_parser.add_argument("--reprocess", type=date.fromisoformat, metavar="YYYY-MM-DD", ...)

   # "schema" subcommand
   schema_parser = subparsers.add_parser("schema", help="Schema registry management")
   schema_sub = schema_parser.add_subparsers(dest="schema_command")
   update_parser = schema_sub.add_parser("update", help="Promote observed schema to registry")
   update_parser.add_argument(
       "--daemon",
       default="all",
       choices=["all", "train_positions", "weather"],
       help="Daemon to update (default: all)"
   )
   update_parser.set_defaults(func=cmd_schema_update)

   # Default behavior: if no subcommand given, run compaction (for systemd backward compat)
   args = parser.parse_args(argv)
   if args.subcommand is None or args.subcommand == "run":
       # existing compaction logic
       reprocess_date = getattr(args, "reprocess", None)
       ...  # rest of existing main() logic
   elif args.subcommand == "schema":
       if not hasattr(args, "func"):
           schema_parser.print_help()
           return
       args.func(args)

3. _REGISTRY_DIR constant (already added in Plan 02) is used here for registry_path.
   Verify it is defined as: Path(__file__).resolve().parents[2] / "schemas"
   (parents[2] from compact.py = src/cta_eta/, so this resolves to src/cta_eta/schemas/)
   Do NOT use parents[3] (that resolves to src/schemas/, which does not exist).
  </action>
  <verify>
`uv run cta-compact --help` — shows subcommands including "schema"
`uv run cta-compact schema update --help` — shows --daemon option
`uv run cta-compact schema --help` — shows "update" subcommand
`uv run cta-compact run --help` — shows --reprocess option
`uv run basedpyright src/cta_eta/data_collection/compaction/compact.py` — 0 errors
  </verify>
  <done>cta-compact schema update command exists, reads latest Parquet schema, overwrites registry file, attempts git commit with fallback to print path. Existing cta-compact (no subcommand) still runs compaction (backward compatible for systemd service). Manual reprocessing uses new form: cta-compact run --reprocess DATE.</done>
</task>

<task type="auto">
  <name>Task 3: Tests for Schema column and schema update command</name>
  <files>tests/monitoring/test_cli.py</files>
  <action>
Add tests to test_cli.py for the new Schema column behavior and the cmd_schema_update command. Read the existing test file first to understand the testing patterns in use, then add new test classes.

Test class TestCompactionSchemaColumn:
  test_schema_column_ok:
    - Create a temp Parquet file with no schema_drift metadata
    - Patch _DEFAULT_COMPACTION_DIR to point to a temp dir structured as:
        {tmpdir}/{daemon}/date={date}/data.parquet
    - Create matching sidecar JSON in tmpdir
    - Call cmd_compaction with patched args (days=7, json=False)
    - Capture stdout
    - Assert "OK" appears in output (Schema column shows OK for clean file)

  test_schema_column_drift:
    - Same setup but add schema_drift=b"true" metadata to the Parquet file:
        table = pa.table({"x": pa.array([1, 2, 3])})
        annotated = table.replace_schema_metadata({b"schema_drift": b"true"})
        pq.write_table(annotated, parquet_path, compression="snappy")
    - Assert "DRIFT" appears in stdout output

  test_schema_column_missing_file:
    - Create sidecar JSON but no Parquet file
    - Assert "?" appears in stdout output (graceful missing-file handling)

Test class TestSchemaUpdateCommand:
  test_schema_update_writes_registry:
    - Create a temp Parquet file with a known schema (e.g., one int64 column)
    - Arrange directory: {tmpdir}/train_positions/date=2026-02-25/data.parquet
    - Create a mock args.daemon = "train_positions"
    - Patch load_config() to return compaction_dir pointing to tmpdir
    - Patch save_registry to capture calls (or use a real temp registry dir)
    - Patch subprocess.run to not actually invoke git
    - Call cmd_schema_update(args)
    - Assert save_registry called with the registry path and a schema matching the Parquet

  test_schema_update_git_commit_attempted:
    - Same setup as above with subprocess.run patched to succeed (check=True, no raise)
    - Assert subprocess.run called twice: once with ["git", "add", ...], once with ["git", "commit", ...]
    - Assert "committed to git" appears in stdout

  test_schema_update_git_fallback_on_failure:
    - Patch subprocess.run to raise subprocess.CalledProcessError
    - Call cmd_schema_update(args) with a valid Parquet
    - Assert "Commit manually" appears in stdout (fallback message)

  test_schema_update_no_parquet_found:
    - Provide a daemon dir that exists but has no data.parquet files
    - Assert "No Parquet file found" appears in stdout, no crash

Note: If test_cli.py already has a TestCompaction class or similar, add tests to it rather than creating a duplicate. Read the file first.
  </action>
  <verify>
`uv run pytest tests/monitoring/test_cli.py -v` — new tests pass alongside existing tests
`uv run pytest` — full suite passes, no regressions
  </verify>
  <done>TestCompactionSchemaColumn (3 tests) and TestSchemaUpdateCommand (4 tests) all pass. Schema column shows OK/DRIFT/? correctly. Schema update writes registry, attempts git commit, and falls back gracefully on failure. No regressions in existing CLI tests.</done>
</task>

</tasks>

<verification>
- `uv run cta-compact --help` — lists run and schema subcommands
- `uv run cta-compact schema update --help` — shows --daemon option
- `uv run cta-compact run --help` — shows --reprocess option
- `uv run cta-monitor compaction --help` — no import errors
- `uv run basedpyright src/cta_eta/monitoring/cli.py src/cta_eta/data_collection/compaction/compact.py` — 0 errors
- `uv run pytest` — full test suite passes
</verification>

<success_criteria>
cta-monitor compaction shows Schema column with OK/DRIFT/? per row. cta-compact schema update reads latest Parquet, saves registry, attempts git commit. Backward compatibility preserved: existing systemd service invocation (no subcommand) continues to run compaction. Manual reprocessing uses new explicit form: cta-compact run --reprocess DATE.
</success_criteria>

<output>
After completion, create `.planning/phases/12-storage-optimization/12-03-SUMMARY.md`
</output>
